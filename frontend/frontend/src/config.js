/**
 * Frontend configuration
 */

export const CONFIG = {
  // Debug mode: shows raw progress % from server + stage name
  DEBUG_UPLOAD_PROGRESS: true,

  // Animation timing for progress bar (ms)
  PROGRESS_ANIMATION_DURATION: 500,
}
